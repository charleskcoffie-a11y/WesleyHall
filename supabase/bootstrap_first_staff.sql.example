-- Run only after the staff member already has an account in Supabase Auth.
-- Replace the email below with the actual first Wesley Hall administrator.

insert into public.wesley_staff_users (user_id, role)
select id, 'admin'
from auth.users
where email = 'YOUR-ADMIN-EMAIL'
on conflict (user_id) do update
set role = excluded.role, active = true;
